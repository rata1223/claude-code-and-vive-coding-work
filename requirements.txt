# Trading Bot Core Dependencies

# Market Data & Technical Analysis
# 2.2.2 (was 2.1.4): pandas-ta-openbb 0.4.24 requires pandas>=2.2.0.
pandas==2.2.2
# pandas-ta-openbb: maintained PyPI fork of pandas-ta. The original `pandas-ta` only
# publishes 0.4.x betas requiring numpy>=2.2.6, which collides with PyPortfolioOpt 1.5.5
# (numpy<2.0.0). The openbb fork pins numpy>=1.26.4 (no upper bound), so it co-resolves
# with PyPortfolioOpt at numpy 1.26.x. Installs as top-level `pandas_ta` (same import name
# as the original), so `import pandas_ta` works unchanged.
pandas-ta-openbb==0.4.24
# 1.26.4 (was 1.24.3): 1.24.3 has no cp312 wheel and pandas 2.1.4 needs numpy>=1.26 on 3.12.
numpy==1.26.4
# 1.17.1 (was 1.11.4): pandas-ta-openbb 0.4.24 requires scipy>=1.15.3.
scipy==1.17.1
yfinance==0.2.36
PyPortfolioOpt==1.5.5

# API & Network Communication
requests==2.33.0
websocket-client==1.7.0
python-dateutil==2.8.2
pytz==2024.1
pykrx==1.0.47
pandas-market-calendars==4.4.2

# Data Storage & Database
redis==5.0.1
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
alembic==1.13.1

# Task Scheduling & Automation
APScheduler==3.10.4

# Notifications
python-telegram-bot==20.7

# Environment Configuration
python-dotenv==1.2.2

# Stage 4+: Backend API & Real-time
flask==3.1.3
flask-cors==6.0.0
flask-socketio==5.3.6
python-socketio==5.16.2
# 4.12.2 (was 4.8.0): python-socketio 5.14.0 requires python-engineio>=4.11.0.
python-engineio==4.13.2

# Production WSGI Server
gunicorn==22.0.0

# Security & Authentication
# pyjwt removed: codebase uses `from jose import jwt` (python-jose), not PyJWT.
# PyJWT is unused and carries HIGH advisories (PYSEC-2025-183, GHSA-752w-5fwx-jx9f).
cryptography==48.0.1

# Monitoring & Logging
python-json-logger==2.0.7

# Stage 6+: Advanced Features
# 7.3 (was 7.1): GHSA — AttributeError.obj + the string module could leak
# protected info in the restricted exec environment. Patched in 7.3.
RestrictedPython==7.3

# Development & Testing (optional)
# pytest==7.4.3
# pytest-asyncio==0.23.3
# black==23.12.0
