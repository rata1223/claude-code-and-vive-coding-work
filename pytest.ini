[pytest]
markers =
    live_paper: opt-in smoke test that hits the real KIS paper (VTS) endpoint over the network. Skipped unless RUN_LIVE_PAPER=1 and KIS paper credentials are set. Not part of the default CI gate.
